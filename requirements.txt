# 核心依赖
# 安装: uv sync (推荐) 或 pip install -r requirements.txt

# 数据获取
tushare>=1.4.0
requests>=2.31
python-dotenv>=1.0

# 数据存储 + 分析
duckdb>=1.5.5
pandas>=3.0
numpy>=1.24

# 配置
pyyaml>=6.0

# 因子挖掘 (可选)
gplearn>=0.4
lightgbm>=4.0

# 缠论核心 (czsc 1.0+)
czsc>=1.0.1
