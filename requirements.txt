# 核心依赖 (2026-07-27)
# 安装: uv pip install -r requirements.txt (推荐, 快) 或 pip install -r requirements.txt

# 因子计算 + 报告
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
requests>=2.31

# 遗传算法挖因子 (Day 9)
gplearn>=0.4

# LightGBM 多因子打分 (Day 11)
lightgbm>=4.0

